import { SI_PUBLIC_CLAIMS, SI_PUBLIC_CONCEPTS, SI_PUBLIC_META, SI_PUBLIC_SOURCES } from '@/lib/atlas/synthetic-intelligence'; import { SYNTHETIC_ATLAS_PATH } from '@/components/SyntheticAtlas';
const base = `https://research.mahastrategies.com${SYNTHETIC_ATLAS_PATH}`;
export async function GET() { return Response.json({ id: 'synthetic-intelligence-atlas-context-pack', title: `${SI_PUBLIC_META.title} context pack`, canonicalUrl: `${base}/context-pack`, release: { version: SI_PUBLIC_META.version, published: SI_PUBLIC_META.datePublished, updated: SI_PUBLIC_META.dateModified, reviewed: SI_PUBLIC_META.lastReviewed, evidenceCutoff: SI_PUBLIC_META.evidenceCutoff, license: SI_PUBLIC_META.license }, scope: SI_PUBLIC_META.scope, inclusions: 'Established and active-research claims with content-verified public sources.', exclusions: ['Forecast scenarios', 'Local methodological artifacts', 'Headline benchmark leaderboards', 'Price trajectories', 'Quantum computing', 'Unreviewed capability forecasts'], counts: { claims: SI_PUBLIC_CLAIMS.length, concepts: SI_PUBLIC_CONCEPTS.length, sources: SI_PUBLIC_SOURCES.length }, endpoints: { manifest: `${base}/metadata.json`, claims: `${base}/claims.json`, sources: `${base}/sources.json`, context: `${base}/context.txt`, contextPack: `${base}/context-pack.json` } }, { headers: { 'Cache-Control': 'public, max-age=3600' } }); }
