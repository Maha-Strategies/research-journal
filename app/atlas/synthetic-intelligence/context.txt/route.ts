import { SI_PUBLIC_CLAIMS, SI_PUBLIC_META, SI_PUBLIC_SOURCES } from '@/lib/atlas/synthetic-intelligence';
import { SYNTHETIC_ATLAS_PATH } from '@/components/SyntheticAtlas';

const SITE_URL = 'https://research.mahastrategies.com';
export async function GET() { const url = `${SITE_URL}${SYNTHETIC_ATLAS_PATH}`; const body = [`# ${SI_PUBLIC_META.title}`, '', SI_PUBLIC_META.description, '', `Version: ${SI_PUBLIC_META.version}`, `Evidence cutoff: ${SI_PUBLIC_META.evidenceCutoff}`, `Last reviewed: ${SI_PUBLIC_META.lastReviewed}`, `Canonical URL: ${url}`, `Claims JSON: ${url}/claims.json`, `Sources JSON: ${url}/sources.json`, '', '## Editorial boundary', 'This first public edition includes only established and active-research claims supported by content-verified public sources. Forecast scenarios, URL-only sources, local audit artifacts, benchmark leaderboards, specific pricing trajectories, and quantum computing are excluded.', '', '## Public claims', ...SI_PUBLIC_CLAIMS.map((claim) => `- ${claim.id} [${claim.status}]: ${claim.claim}`), '', '## Sources', ...SI_PUBLIC_SOURCES.map((source) => `- ${source.title} (${source.year}) — ${source.url}`), ''].join('\n'); return new Response(body, { headers: { 'Content-Type': 'text/plain; charset=utf-8', 'Cache-Control': 'public, max-age=3600' } }); }
