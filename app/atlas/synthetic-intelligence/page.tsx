import type { Metadata } from 'next';
import Link from 'next/link';

import { AtlasNav, AtlasShell, ClaimCard, ConceptCard, SourceCard, SYNTHETIC_ATLAS_PATH } from '@/components/SyntheticAtlas';
import { SI_PUBLIC_CLAIMS, SI_PUBLIC_CONCEPTS, SI_PUBLIC_META, SI_PUBLIC_SOURCES, SI_STATUSES, getSiStatus } from '@/lib/atlas/synthetic-intelligence';

const SITE_URL = 'https://research.mahastrategies.com';
const URL = `${SITE_URL}${SYNTHETIC_ATLAS_PATH}`;

export const metadata: Metadata = { title: `${SI_PUBLIC_META.title}`, description: SI_PUBLIC_META.description, alternates: { canonical: SYNTHETIC_ATLAS_PATH }, robots: { index: true, follow: true, 'max-image-preview': 'large' }, openGraph: { type: 'article', url: URL, title: SI_PUBLIC_META.title, description: SI_PUBLIC_META.description }, twitter: { card: 'summary', title: SI_PUBLIC_META.title, description: SI_PUBLIC_META.description } };

export default function SyntheticIntelligenceAtlasPage() {
  const ld = { '@context': 'https://schema.org', '@graph': [{ '@type': 'LearningResource', '@id': `${URL}#atlas`, name: SI_PUBLIC_META.title, description: SI_PUBLIC_META.description, url: URL, inLanguage: 'en', isAccessibleForFree: true, license: SI_PUBLIC_META.license, creativeWorkStatus: 'Non-peer-reviewed educational research map', datePublished: SI_PUBLIC_META.datePublished, dateModified: SI_PUBLIC_META.dateModified, author: { '@type': 'Person', name: 'Mayone Maha Rajan', url: 'https://www.mayonemaharajan.com' }, publisher: { '@type': 'Organization', name: 'Maha Strategies', url: 'https://www.mahastrategies.com' }, hasPart: SI_PUBLIC_CLAIMS.map((claim) => ({ '@type': 'Claim', identifier: claim.id, url: `${URL}/claims/${claim.id}`, text: claim.claim })), encoding: [{ '@type': 'MediaObject', encodingFormat: 'application/json', contentUrl: `${URL}/metadata.json` }, { '@type': 'MediaObject', encodingFormat: 'application/json', contentUrl: `${URL}/claims.json` }, { '@type': 'MediaObject', encodingFormat: 'application/json', contentUrl: `${URL}/sources.json` }] }] };
  return <AtlasShell><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(ld) }} /><div className="mx-auto max-w-5xl"><AtlasNav /><header className="max-w-3xl"><p className="font-mono text-[10px] uppercase tracking-[0.18em] text-cyan-300">Source-bounded research map · evidence through {SI_PUBLIC_META.evidenceCutoff}</p><h1 className="mt-5 text-3xl font-light uppercase leading-tight tracking-wide text-white md:text-5xl">Synthetic Intelligence <span className="text-zinc-500">Atlas</span></h1><p className="mt-7 border-l border-cyan-400/40 pl-4 text-base leading-relaxed text-zinc-400">{SI_PUBLIC_META.description} This first edition privileges what can be checked over what sounds current.</p><div className="mt-7 flex flex-wrap items-center gap-3"><p className="inline-block border border-emerald-400/35 bg-emerald-400/5 px-4 py-2 font-mono text-[10px] uppercase tracking-widest text-emerald-200">{SI_PUBLIC_META.statusBadge}</p>{/* The context-pack PAGE was in the sitemap but linked from nowhere — only its .json endpoint was reachable. This is the human entry point, matching how the de Sitter atlas surfaces its own pack. */}<Link href={`${SYNTHETIC_ATLAS_PATH}/context-pack`} className="inline-block border border-cyan-400/50 px-4 py-2 font-mono text-[10px] uppercase tracking-widest text-cyan-200 hover:bg-cyan-400 hover:text-zinc-950">Context pack →</Link></div></header><section className="mt-14 grid gap-4 md:grid-cols-3"><div className="border border-zinc-800 p-5"><p className="font-mono text-[10px] uppercase tracking-widest text-zinc-500">Public claims</p><p className="mt-3 text-3xl font-light text-white">{SI_PUBLIC_CLAIMS.length}</p></div><div className="border border-zinc-800 p-5"><p className="font-mono text-[10px] uppercase tracking-widest text-zinc-500">Verified sources</p><p className="mt-3 text-3xl font-light text-white">{SI_PUBLIC_SOURCES.length}</p></div><div className="border border-zinc-800 p-5"><p className="font-mono text-[10px] uppercase tracking-widest text-zinc-500">Orientation concepts</p><p className="mt-3 text-3xl font-light text-white">{SI_PUBLIC_CONCEPTS.length}</p></div></section><section className="mt-16"><h2 className="border-b border-zinc-800 pb-3 font-mono text-[10px] uppercase tracking-widest text-zinc-500">How to read this Atlas</h2><div className="mt-6 grid gap-4 md:grid-cols-2">{SI_STATUSES.filter((status) => status.id === 'established' || status.id === 'active').map((status) => <div key={status.id} className="border border-zinc-800 bg-[#121214] p-5"><p className="font-mono text-[10px] uppercase tracking-widest text-cyan-300">{status.label}</p><p className="mt-3 text-sm leading-relaxed text-zinc-400">{status.definition}</p></div>)}</div><p className="mt-5 text-sm leading-relaxed text-zinc-500">Forecast scenarios and a local audit exhibit are deliberately withheld from this first public edition. URL-only records, benchmark leaderboards, specific price trajectories, and quantum computing are outside its public evidence set.</p></section><section className="mt-16"><div className="flex flex-wrap items-end justify-between gap-4 border-b border-zinc-800 pb-3"><h2 className="font-mono text-[10px] uppercase tracking-widest text-zinc-500">Claim ledger · {SI_PUBLIC_CLAIMS.length}</h2><a href={`${SYNTHETIC_ATLAS_PATH}/claims.json`} className="font-mono text-[10px] uppercase tracking-widest text-cyan-300 hover:text-white">claims.json ↗</a></div><div className="mt-6 grid gap-4">{SI_PUBLIC_CLAIMS.map((claim) => <ClaimCard key={claim.id} claim={claim} statusLabel={getSiStatus(claim.status).label} />)}</div></section><section className="mt-16 grid gap-12 lg:grid-cols-2"><div><div className="flex items-end justify-between gap-3 border-b border-zinc-800 pb-3"><h2 className="font-mono text-[10px] uppercase tracking-widest text-zinc-500">Concept library</h2><Link href={`${SYNTHETIC_ATLAS_PATH}/concepts`} className="font-mono text-[10px] uppercase tracking-widest text-cyan-300 hover:text-white">All concepts →</Link></div><div className="mt-6 grid gap-4">{SI_PUBLIC_CONCEPTS.slice(0, 4).map((concept) => <ConceptCard key={concept.id} concept={concept} />)}</div></div><div><div className="flex items-end justify-between gap-3 border-b border-zinc-800 pb-3"><h2 className="font-mono text-[10px] uppercase tracking-widest text-zinc-500">Source library</h2><Link href={`${SYNTHETIC_ATLAS_PATH}/sources`} className="font-mono text-[10px] uppercase tracking-widest text-cyan-300 hover:text-white">All sources →</Link></div><div className="mt-6 grid gap-4">{SI_PUBLIC_SOURCES.slice(0, 4).map((source) => <SourceCard key={source.id} source={source} />)}</div></div></section><footer className="mt-16 border-t border-zinc-800 pt-7 text-xs leading-relaxed text-zinc-500">Educational, non-peer-reviewed research map. A claim’s status and limitations are part of the claim. This Atlas is not investment, safety, deployment, or technology-adoption advice. Machine-readable: <a className="text-cyan-300 underline" href={`${SYNTHETIC_ATLAS_PATH}/metadata.json`}>metadata.json</a> · <a className="text-cyan-300 underline" href={`${SYNTHETIC_ATLAS_PATH}/context.txt`}>context.txt</a>.</footer></div></AtlasShell>;
}
