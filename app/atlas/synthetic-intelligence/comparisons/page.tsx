import type { Metadata } from 'next'; import Link from 'next/link'; import { AtlasNav, AtlasShell, SYNTHETIC_ATLAS_PATH } from '@/components/SyntheticAtlas'; import { SI_PUBLIC_META } from '@/lib/atlas/synthetic-intelligence'; import { SI_COMPARISONS } from '@/lib/atlas/synthetic-intelligence-decision';
const URL = `https://research.mahastrategies.com${SYNTHETIC_ATLAS_PATH}/comparisons`; export const metadata: Metadata = { title: `Decision Comparisons | ${SI_PUBLIC_META.title}`, description: 'Bounded, evidence-linked distinctions for readers assessing LLMs and agentic systems.', alternates: { canonical: `${SYNTHETIC_ATLAS_PATH}/comparisons` }, robots: { index: true, follow: true } };
export default function ComparisonsPage() { const ld = { '@context': 'https://schema.org', '@type': 'CollectionPage', '@id': `${URL}#page`, name: `${SI_PUBLIC_META.title}: Evidence & Decision Layer`, url: URL, hasPart: SI_COMPARISONS.map((comparison) => ({ '@type': 'Article', name: comparison.title, url: `${URL}/${comparison.slug}` })) }; return <AtlasShell><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(ld) }} /><div className="mx-auto max-w-5xl"><AtlasNav back={{ href: SYNTHETIC_ATLAS_PATH, label: SI_PUBLIC_META.shortTitle }} /><header className="max-w-3xl"><p className="font-mono text-[10px] uppercase tracking-widest text-cyan-300">Evidence & decision layer · v{SI_PUBLIC_META.version} · source-bounded</p><h1 className="mt-4 text-3xl font-light text-white md:text-5xl">Decision-relevant comparisons.</h1><p className="mt-6 text-sm leading-relaxed text-zinc-400">Each comparison makes one bounded distinction using existing Atlas claims and sources. They are not product rankings, deployment recommendations, or forecasts.</p></header><section className="mt-12 grid gap-4 md:grid-cols-2">{SI_COMPARISONS.map((comparison) => <article key={comparison.slug} className="border border-zinc-800 bg-[#121214] p-5"><h2 className="text-xl font-light text-white"><Link href={`${SYNTHETIC_ATLAS_PATH}/comparisons/${comparison.slug}`} className="hover:text-cyan-300">{comparison.title}</Link></h2><p className="mt-3 text-sm leading-relaxed text-zinc-400">{comparison.question}</p><p className="mt-4 text-xs leading-relaxed text-zinc-500">{comparison.decisionUse}</p><Link href={`${SYNTHETIC_ATLAS_PATH}/comparisons/${comparison.slug}`} className="mt-5 inline-block font-mono text-[10px] uppercase tracking-widest text-cyan-300">Read comparison →</Link></article>)}</section></div></AtlasShell>; }
