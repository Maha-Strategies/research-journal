import { QC_CLAIMS, QC_META, QC_SOURCES } from '@/lib/atlas/quantum-computing'; import { QUANTUM_ATLAS_PATH } from '@/components/QuantumAtlas';
const base = `https://research.mahastrategies.com${QUANTUM_ATLAS_PATH}`;
export async function GET() { const body = [`# ${QC_META.title}`, '', QC_META.description, '', `Version: ${QC_META.version}`, `Evidence cutoff: ${QC_META.evidenceCutoff}`, `Canonical URL: ${base}`, `Claims JSON: ${base}/claims.json`, `Sources JSON: ${base}/sources.json`, `Methodology: ${base}/methodology`, '', '## Boundary', 'This edition includes source-bounded claims only. It excludes forecasts, investment advice, platform rankings, vendor roadmaps as evidence, and performance leaderboards.', '', '## Claims', ...QC_CLAIMS.map((c) => `- ${c.id} [${c.status}]: ${c.claim}`), '', '## Sources', ...QC_SOURCES.map((s) => `- ${s.title} (${s.year}) — ${s.url}`), ''].join('\n'); return new Response(body, { headers: { 'Content-Type': 'text/plain; charset=utf-8', 'Cache-Control': 'public, max-age=3600' } }); }
