import type { Metadata } from 'next';
import Link from 'next/link';
import { AtlasNav, AtlasShell, ClaimCard, ConceptCard, QUANTUM_ATLAS_PATH, SourceCard } from '@/components/QuantumAtlas';
import { QC_CLAIMS, QC_CONCEPTS, QC_META, QC_SOURCES, QC_STATUSES, getQcStatus } from '@/lib/atlas/quantum-computing';
const SITE_URL = 'https://research.mahastrategies.com'; const URL = `${SITE_URL}${QUANTUM_ATLAS_PATH}`;
export const metadata: Metadata = { title: QC_META.title, description: QC_META.description, alternates: { canonical: QUANTUM_ATLAS_PATH }, robots: { index: true, follow: true }, openGraph: { type: 'article', url: URL, title: QC_META.title, description: QC_META.description } };
export default function QuantumAtlasPage() { const ld = { '@context': 'https://schema.org', '@type': 'LearningResource', '@id': `${URL}#atlas`, name: QC_META.title, description: QC_META.description, url: URL, datePublished: QC_META.datePublished, dateModified: QC_META.dateModified, creativeWorkStatus: 'Non-peer-reviewed educational research map', isAccessibleForFree: true, license: QC_META.license, hasPart: QC_CLAIMS.map((c) => ({ '@type': 'Claim', identifier: c.id, url: `${URL}/claims/${c.id}`, text: c.claim })), encoding: ['metadata.json', 'claims.json', 'sources.json'].map((path) => ({ '@type': 'MediaObject', encodingFormat: 'application/json', contentUrl: `${URL}/${path}` })) }; return <AtlasShell><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(ld) }} /><div className="mx-auto max-w-5xl"><AtlasNav /><header className="max-w-3xl"><p className="font-mono text-[10px] uppercase tracking-[0.18em] text-violet-300">Source-led research library · evidence through {QC_META.evidenceCutoff}</p><h1 className="mt-5 text-3xl font-light uppercase leading-tight tracking-wide text-white md:text-5xl">Quantum Computing <span className="text-zinc-500">Atlas</span></h1><p className="mt-7 border-l border-violet-400/40 pl-4 text-base leading-relaxed text-zinc-400">{QC_META.description} It favours explicit scope, primary sources, and comparative evidence over technology narratives.</p><p className="mt-7 inline-block border border-violet-400/35 bg-violet-400/5 px-4 py-2 font-mono text-[10px] uppercase tracking-widest text-violet-200">{QC_META.statusBadge}</p></header><section className="mt-14 grid gap-4 md:grid-cols-3">{[['Claim records', QC_CLAIMS.length], ['Verified sources', QC_SOURCES.length], ['Concept records', QC_CONCEPTS.length]].map(([label, count]) => <div key={String(label)} className="border border-zinc-800 p-5"><p className="font-mono text-[10px] uppercase tracking-widest text-zinc-500">{label}</p><p className="mt-3 text-3xl font-light text-white">{count}</p></div>)}</section><section className="mt-16"><h2 className="border-b border-zinc-800 pb-3 font-mono text-[10px] uppercase tracking-widest text-zinc-500">Evidence labels</h2><div className="mt-6 grid gap-4 md:grid-cols-2">{QC_STATUSES.map((s) => <div key={s.id} className="border border-zinc-800 bg-[#121214] p-5"><p className="font-mono text-[10px] uppercase tracking-widest text-violet-300">{s.label}</p><p className="mt-3 text-sm leading-relaxed text-zinc-400">{s.definition}</p></div>)}</div></section><section className="mt-16"><div className="flex justify-between border-b border-zinc-800 pb-3"><h2 className="font-mono text-[10px] uppercase tracking-widest text-zinc-500">Claim ledger · {QC_CLAIMS.length}</h2><a href={`${QUANTUM_ATLAS_PATH}/claims.json`} className="font-mono text-[10px] text-violet-300">claims.json ↗</a></div><div className="mt-6 grid gap-4">{QC_CLAIMS.map((c) => <ClaimCard key={c.id} claim={c} label={getQcStatus(c.status).label} />)}</div></section><section className="mt-16 grid gap-12 lg:grid-cols-2"><div><div className="flex justify-between border-b border-zinc-800 pb-3"><h2 className="font-mono text-[10px] uppercase tracking-widest text-zinc-500">Concept library</h2><Link href={`${QUANTUM_ATLAS_PATH}/concepts`} className="font-mono text-[10px] text-violet-300">All concepts →</Link></div><div className="mt-6 grid gap-4">{QC_CONCEPTS.slice(0, 4).map((c) => <ConceptCard key={c.id} concept={c} />)}</div></div><div><div className="flex justify-between border-b border-zinc-800 pb-3"><h2 className="font-mono text-[10px] uppercase tracking-widest text-zinc-500">Source library</h2><Link href={`${QUANTUM_ATLAS_PATH}/sources`} className="font-mono text-[10px] text-violet-300">All sources →</Link></div><div className="mt-6 grid gap-4">{QC_SOURCES.slice(0, 4).map((s) => <SourceCard key={s.id} source={s} />)}</div></div></section><footer className="mt-16 border-t border-zinc-800 pt-7 text-xs leading-relaxed text-zinc-500">Educational, non-peer-reviewed research map. <Link className="text-violet-300 underline" href={`${QUANTUM_ATLAS_PATH}/methodology`}>Methodology and update policy</Link> · <a className="text-violet-300 underline" href={`${QUANTUM_ATLAS_PATH}/metadata.json`}>metadata.json</a> · <a className="text-violet-300 underline" href={`${QUANTUM_ATLAS_PATH}/context.txt`}>context.txt</a>.</footer></div></AtlasShell>; }
